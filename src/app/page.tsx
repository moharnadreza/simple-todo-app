import { PageWrapper } from "@/components";
import TodoContainer from "@/features/todos/components/TodoContainer";

const Home = () => {
  return (
    <PageWrapper>
      <TodoContainer />
    </PageWrapper>
  );
};

export default Home;
